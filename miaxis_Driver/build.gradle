configurations.maybeCreate("default")
artifacts.add("default", file("Miaxis_Driver.aar"))

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = "com.github.EdwardThompson-CodeDemon"
                artifactId = "Realm.miaxis_Driver"
//                version = "1.0.326"
            }
        }
    }
}